# Weather App 🌤️

A simple **Python desktop application** that shows the current weather for any city using the **OpenWeatherMap API**. The app displays the temperature, weather description, and a corresponding emoji for easy visualization. Built with **PyQt5** for the GUI.

---

## Features

- Enter a city name to get its current weather.
- Displays:
  - Temperature (°C)
  - Weather description
  - Weather emoji (e.g., ☀️, 🌧️, ❄️)
- Handles common HTTP and network errors gracefully.
- Exit button conveniently placed at the bottom-right corner.
- Customizable GUI with large fonts and emoji support.

---

## Usage

1. Run the app:

```bash
main.py
```

2. Enter a city name in the input field and click **Get Weather**.
3. The temperature, weather description, and emoji will update automatically.
4. Click **Exit** to close the app.

---

## API Key

This app uses the [OpenWeatherMap API](https://openweathermap.org/api).
